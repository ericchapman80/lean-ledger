# Lean Ledger — local development dependencies.
# Install with: brew bundle
# Update with:  brew bundle --force cleanup   (removes anything not listed)

# Postgres 16 — used when running fully offline against a local DB instead of Neon.
brew "postgresql@16"

# Optional GUI clients (uncomment to install):
# cask "tableplus"
# cask "postico"
