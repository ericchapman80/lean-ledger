# Neon Postgres connection string
# Vercel: pull with `vercel env pull .env.local` after linking the Neon integration
# Local dev: paste your Neon dev branch URL here (Neon dashboard → Connection Details)
DATABASE_URL=postgresql://user:password@host.neon.tech/dbname?sslmode=require
